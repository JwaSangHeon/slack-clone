import Workspace from '@layouts/Workspace';
import React from 'react';

const Channel = () => {
  return (
    <Workspace>
      <div> 축하합니다. 로그인되었습니다.</div>
    </Workspace>
  );
};

export default Channel;
